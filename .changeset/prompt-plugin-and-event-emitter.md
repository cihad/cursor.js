---
"@cursor.js/core": minor
---

feat: add `PromptPlugin` with customizable UI, and introduce asynchronous event emitter (`emitAsync`) for seamless speech integration with plugins like `SayPlugin` and `SpeechPlugin`.
