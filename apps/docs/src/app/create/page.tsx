import { ClientPage } from './ClientPage';

export const metadata = {
  title: 'Create Animation - Cursor.js',
  description: 'Create and configure Cursor.js animations',
};

export default function CreatePage() {
  return <ClientPage />;
}
