---
"@cursor.js/core": patch
---

fix: correct the built-in pointer cursor hotspot

The built-in ThemePlugin pointer cursor now uses the intended hotspot offset so clicks and hover states align with the visible tip.
